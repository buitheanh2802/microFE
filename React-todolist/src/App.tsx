import React from 'react';

type Props = {};

const App: React.FC<Props> = (props): React.ReactNode => {
    return (
        <React.Fragment>
            hello world
        </React.Fragment>
    )
};

export default App;